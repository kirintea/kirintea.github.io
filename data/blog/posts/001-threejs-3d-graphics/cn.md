## 介绍

Three.js 是一个强大的 JavaScript 库，简化了 WebGL 的使用过程。它提供了一个更高级的 API，用于在浏览器中创建 3D 图形。

## 入门

要开始使用 Three.js，您需要在项目中包含该库，并设置一个包含相机、渲染器和一些几何体的基本场景。

## 关键概念

- **场景** - 所有 3D 对象的容器
- **相机** - 决定查看者看到什么
- **渲染器** - 将场景绘制到画布上
- **几何体** - 定义对象的形状
- **材质** - 定义对象的外观
